{
  "name": "monorepos-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "pnpm i",
    "commit": "git-cz",
    "changeset": "changeset && changeset version && pnpm i",
    "cleanup": "rimraf 'packages/*/{lib,dist,es,node_modules}' && rimraf node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "commitizen": "^4.3.0",
    "rimraf": "^4.4.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
